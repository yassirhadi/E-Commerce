{% extends 'base.html.twig' %}

{% block title %}Modifier le cours{% endblock %}

{% block body %}
    <h1>Modifier le cours</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('course_show', {'id': course.id}) }}">Retour au cours</a>
{% endblock %}
